//import ImmutableSharedStateDemo.UserValidator22.{hasLongName, isAdult, isAdultAndHasLongName}
import com.evolutiongaming.bootcamp.basics.LinkedList.ImplicitParams.l


object ImmutableSharedStateDemo extends App {
//  case class User(name: String, age: Int)
//
//  trait UserValidator {
//    def apply(user: User): Boolean
//  }
//
//  object UserValidator22 {
//    val isAdult: UserValidator = _.age >= 18
//    val hasLongName: UserValidator = _.name.length >= 10
//    val isAdultAndHasLongName: UserValidator = user => isAdult(user) && hasLongName(user)
//  }
//
//  def processUser(user: User)(userValidator: UserValidator): Unit =
//    if (userValidator(user)) println("You may proceed")
//    else println("Halt! Intruder!")
//
//  val user = User("Martin", 5)
//
//  processUser(user)(isAdult)
//  processUser(user)(hasLongName)
//  processUser(user)(isAdultAndHasLongName)

//  val l: List[Option[Int]] = List(Option(1), Option(null), Option(3), Option(null), Option(5))
//  println(l.map(opt => opt.map(_ + 2)))
//  println(Option(null))
}
